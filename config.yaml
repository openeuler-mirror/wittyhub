database:
  host: "localhost"
  port: 5432
  user: "wittyhub"
  password: "wittyhub_secret"
  dbname: "wittyhub"
  sslmode: "disable"

storage:
  type: "local"
  local_path: "./data/skills"
  github_token: ""

security:
  # Skillspector (Jenkins-based scanner) — sensitive fields via env vars
  skillspector_enabled: true
  skillspector_jenkins_url: "http://120.26.120.159:18080"

ai:
  embedding_model: "bge-base-zh-v1.5"
  embedding_host: "http://localhost:8081"
  embedding_dimension: 768
  enable_semantic_search: true

app:
  host: "0.0.0.0"
  port: 8080
  cors_origins:
    - "*"

logging:
  level: "INFO"
  format: "json"
